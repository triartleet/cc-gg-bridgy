{
  // Centered logo in README uses <p>/<img> — the GitHub-README idiom.
  "MD033": { "allowed_elements": ["div", "p", "img", "a"] },
  // Prose wrapping is the editor's job, not a lint gate.
  "MD013": false
}
